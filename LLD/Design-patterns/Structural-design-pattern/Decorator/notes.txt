If we have a scenario where we add properties/features to a base entity at runtime where the final output depends on
the output of the base, consider using `decorator` design pattern

you wrap it inside another class (called a decorator) that adds extra behavior.